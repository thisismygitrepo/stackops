interface:
  display_name: "Workflows"
  short_description: "Coordinate Herdr workflows and wt-isolated agents."
  default_prompt: "Use $workflows to hand over work or coordinate Herdr-managed parallel agents. For parallel-isolated-agents, use wt/Worktrunk for temporary isolated worktrees and keep the source repo's git history clean until the user asks to integrate work."
