interface:
  display_name: "Workflows"
  short_description: "Use handover, parallel-agents, or parallel-isolated-agents workflows for herdr-managed agents."
  default_prompt: "Use workflows `handover` to transfer active work, `parallel-agents` to coordinate external interactive agents, or `parallel-isolated-agents` to create isolated worktree agents through herdr."
