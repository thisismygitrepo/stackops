interface:
  display_name: "Workflows"
  short_description: "Coordinate Herdr handoffs, iter loops, and agents."
  default_prompt: "Use $workflows to hand over work, run an iter improvement chain, or coordinate Herdr-managed parallel agents with optional wt/Worktrunk isolation."
