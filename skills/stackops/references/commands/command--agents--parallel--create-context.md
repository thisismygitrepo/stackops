# StackOps Command Reference: `agents parallel create-context`

Regenerated from `src/stackops/scripts/python/graph/cli_graph.json` on 2026-06-21.

This page expands exactly one level below `agents parallel create-context`. Follow child links one command segment at a time.

## Current Command

- Path: `agents parallel create-context`
- Kind: `command`
- Source: `src/stackops/scripts/python/agents_parallel_commands.py` -> `create_context`
- Help: `Run one prompt and ask the selected agent to persist a context file for the job.`

## Help Commands

- `UV_CACHE_DIR=/tmp/uv-cache uv run agents parallel create-context --help`
- `UV_CACHE_DIR=/tmp/uv-cache uv run stackops agents parallel create-context --help`

## Immediate Children

- No child command references. Use `UV_CACHE_DIR=/tmp/uv-cache uv run agents parallel create-context --help` for options and inspect `src/stackops/scripts/python/graph/cli_graph.json` for full metadata.

## Notes

- Developer-only command groups under `devops self` depend on the developer checkout at `~/code/stackops`.
- Callback groups are invoked as the group command itself when their command reference has no children.
- The generated graph stores aliases on each node. Use `src/stackops/scripts/python/graph/cli_graph.json` when alias details matter.
- Docs may lag source. Prefer command paths and behavior verified from current Typer source and `--help` output.
