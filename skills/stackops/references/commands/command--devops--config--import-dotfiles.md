# StackOps Command Reference: `devops config import-dotfiles`

Regenerated from `src/stackops/scripts/python/graph/cli_graph.json` on 2026-06-21.

This page expands exactly one level below `devops config import-dotfiles`. Follow child links one command segment at a time.

## Current Command

- Path: `devops config import-dotfiles`
- Kind: `command`
- Source: `src/stackops/scripts/python/helpers/helpers_devops/cli_config_dotfile_transfer.py` -> `import_dotfiles`
- Help: `📥 <I> Import dotfiles from exported archive.`

## Help Commands

- `UV_CACHE_DIR=/tmp/uv-cache uv run devops config import-dotfiles --help`
- `UV_CACHE_DIR=/tmp/uv-cache uv run stackops devops config import-dotfiles --help`

## Immediate Children

- No child command references. Use `UV_CACHE_DIR=/tmp/uv-cache uv run devops config import-dotfiles --help` for options and inspect `src/stackops/scripts/python/graph/cli_graph.json` for full metadata.

## Notes

- Developer-only command groups under `devops self` depend on the developer checkout at `~/code/stackops`.
- Callback groups are invoked as the group command itself when their command reference has no children.
- The generated graph stores aliases on each node. Use `src/stackops/scripts/python/graph/cli_graph.json` when alias details matter.
- Docs may lag source. Prefer command paths and behavior verified from current Typer source and `--help` output.
