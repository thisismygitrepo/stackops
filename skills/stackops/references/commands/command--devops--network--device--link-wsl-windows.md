# StackOps Command Reference: `devops network device link-wsl-windows`

Regenerated from `src/stackops/scripts/python/graph/cli_graph.json` on 2026-06-21.

This page expands exactly one level below `devops network device link-wsl-windows`. Follow child links one command segment at a time.

## Current Command

- Path: `devops network device link-wsl-windows`
- Kind: `command`
- Source: `src/stackops/scripts/python/helpers/helpers_devops/cli_device.py` -> `link_wsl_and_windows_home`
- Help: `🔗 <l> Link WSL home and Windows home directories.`

## Help Commands

- `UV_CACHE_DIR=/tmp/uv-cache uv run devops network device link-wsl-windows --help`
- `UV_CACHE_DIR=/tmp/uv-cache uv run stackops devops network device link-wsl-windows --help`

## Immediate Children

- No child command references. Use `UV_CACHE_DIR=/tmp/uv-cache uv run devops network device link-wsl-windows --help` for options and inspect `src/stackops/scripts/python/graph/cli_graph.json` for full metadata.

## Notes

- Developer-only command groups under `devops self` depend on the developer checkout at `~/code/stackops`.
- Callback groups are invoked as the group command itself when their command reference has no children.
- The generated graph stores aliases on each node. Use `src/stackops/scripts/python/graph/cli_graph.json` when alias details matter.
- Docs may lag source. Prefer command paths and behavior verified from current Typer source and `--help` output.
