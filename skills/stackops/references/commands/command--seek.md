# StackOps Command Reference: `seek`

Regenerated from `src/stackops/scripts/python/graph/cli_graph.json` on 2026-06-21.

This page expands exactly one level below `seek`. Follow child links one command segment at a time.

## Current Command

- Path: `seek`
- Kind: `group`
- Source: `src/stackops/scripts/python/stackops_entry.py` -> `stackops.scripts.python.seek.get_app` via `seek`
- Help: `<s> Search across files, text matches, and code symbols`

## Help Commands

- `UV_CACHE_DIR=/tmp/uv-cache uv run seek --help`
- `UV_CACHE_DIR=/tmp/uv-cache uv run stackops seek --help`

## Immediate Children

- [`seek seek`](command--seek--seek.md) - command with no child commands. Help: `seek across files, text matches, and code symbols.`.

## Notes

- Developer-only command groups under `devops self` depend on the developer checkout at `~/code/stackops`.
- Callback groups are invoked as the group command itself when their command reference has no children.
- The generated graph stores aliases on each node. Use `src/stackops/scripts/python/graph/cli_graph.json` when alias details matter.
- Docs may lag source. Prefer command paths and behavior verified from current Typer source and `--help` output.
