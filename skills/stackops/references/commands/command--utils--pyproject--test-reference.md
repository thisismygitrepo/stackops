# StackOps Command Reference: `utils pyproject test-reference`

Regenerated from `src/stackops/scripts/python/graph/cli_graph.json` on 2026-06-21.

This page expands exactly one level below `utils pyproject test-reference`. Follow child links one command segment at a time.

## Current Command

- Path: `utils pyproject test-reference`
- Kind: `command`
- Source: `src/stackops/scripts/python/helpers/helpers_utils/pyproject_utils_commands_check.py` -> `reference_test`
- Help: `🔎 <r> Validate _PATH_REFERENCE targets in a repository.`

## Help Commands

- `UV_CACHE_DIR=/tmp/uv-cache uv run utils pyproject test-reference --help`
- `UV_CACHE_DIR=/tmp/uv-cache uv run stackops utils pyproject test-reference --help`

## Immediate Children

- No child command references. Use `UV_CACHE_DIR=/tmp/uv-cache uv run utils pyproject test-reference --help` for options and inspect `src/stackops/scripts/python/graph/cli_graph.json` for full metadata.

## Notes

- Developer-only command groups under `devops self` depend on the developer checkout at `~/code/stackops`.
- Callback groups are invoked as the group command itself when their command reference has no children.
- The generated graph stores aliases on each node. Use `src/stackops/scripts/python/graph/cli_graph.json` when alias details matter.
- Docs may lag source. Prefer command paths and behavior verified from current Typer source and `--help` output.
