interface:
  display_name: "Agent-Ops"
  short_description: "Coordinate Herdr agent operations and iteration."
  default_prompt: "Use $agent-ops to hand over work, run goal-directed iterative improvements that stop when completion criteria are met, or coordinate Herdr-managed parallel agents."
policy:
  allow_implicit_invocation: false
