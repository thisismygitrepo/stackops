interface:
  display_name: "AgentOps"
  short_description: "Coordinate Herdr agent operations and iteration."
  default_prompt: "Use $agentops to hand over work, run goal-directed iterative improvements that stop when completion criteria are met, or coordinate Herdr-managed parallel agents."
