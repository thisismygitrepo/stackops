{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/thisismygitrepo/stackops/schemas/config.schema.json",
  "title": "StackOps User Config Schema",
  "version": "1.0.0",
  "description": "Schema for the StackOps user configuration file at ~/dotfiles/stackops/config/config.json.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional link to the JSON Schema used for this file."
    },
    "version": {
      "type": "string",
      "description": "Config format version.",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
      "examples": ["1.0.0"]
    },
    "default_rclone_config": {
      "type": "string",
      "description": "Default rclone remote/profile name.",
      "minLength": 1
    },
    "default_email_config": {
      "type": "string",
      "description": "Default email profile name used to send notifications.",
      "minLength": 1
    },
    "default_email_address": {
      "type": "string",
      "description": "Default recipient email address used for notifications.",
      "format": "email",
      "minLength": 1
    }
  },
  "required": ["version", "default_rclone_config", "default_email_config", "default_email_address"]
}
