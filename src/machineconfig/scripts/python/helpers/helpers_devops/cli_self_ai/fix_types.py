
# 
#  a p c  -s "### Diagnostic" -p "please fix those, but don't be so pedantic because the linter and type checker are not perfect and occasionally give false positives" -l 10 -a codex -C ./.ai/linters/issues_mypy.md
# t r -f  ./.ai/agents/AI_Agents/layout.json  --max-tabs-per-layout 50 --on-conflict restart
